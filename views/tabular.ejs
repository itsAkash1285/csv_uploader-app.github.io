<!DOCTYPE html>
<html>
<head>
	<meta charset="utf-8">
	<meta name="viewport" content="width=device-width, initial-scale=1">
	<!-- bootstrap link -->
	<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
	<!-- Local css -->
	<link rel="stylesheet" type="text/css" href="/css/tabular.css">
	<!-- Font-Awesome Link -->
	<script src="https://kit.fontawesome.com/1dd5c5cd8c.js" crossorigin="anonymous"></script>
	<title>CSV Uploader | Tabular</title>
</head>
<body>
	<main>
		<header>
            <div id="home-button">
                <a href="/">Home</a>
            </div>
            <form id="searchForm" action="javascript:search();">
                <div class="input-group">
                   <input type="text" id="searchItem" class="form-control" placeholder="Search data...">
                   <button id="go" class="btn btn-dark" type="button" 
                        onclick="document.getElementById('searchForm').submit();">Go
                   </button>
                </div>
             </form>
			 <div class="rows">
				<a><p id="total-rows">Total Rows: <%= csvData.length%></p></a>
			 </div>
        </header>
		<div id='table-container'>
			<table style="width:100%">
				<tr>
					<% for (const [key, value] of Object.entries(csvData[0])) { %>
					   <th><%= key %></th>
					<% } %>
				</tr>
				<% for(let i=0;i<csvData.length;i++) { %>
				<tr id="row-<%= i %>">
					<% for(const [key, value] of Object.entries(csvData[i])) { %>
					    <td class="col-md-2"><%= value %></td>
				    <% } %>
				</tr>
				<% } %>
			</table>
		</div>
	</main>

	<!-- bootstrap bundle script CDN -->
	<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p" crossorigin="anonymous"></script>
	<script type="text/javascript" src="/js/tabular.js"></script>
</body>
</html>